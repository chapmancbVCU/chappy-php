You need to create profileImage directory here with 777 permissions and
"chown -R $username:daemon profileImage/" to get profile image support to 
work when using XAMPP.  Permissions/ownership/group setting will differ
depending on environment.